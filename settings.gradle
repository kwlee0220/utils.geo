rootProject.name = 'utils.geo'

ext.common_dir = '..'
['utils','utils.record'].each {
	include ":$it"
	project(":$it").projectDir = file("${common_dir}/$it")
}

ext.record_dir = "../../record"
['record', 'record.shp'].each {
	include ":$it"
	project(":$it").projectDir = file("${record_dir}/$it")
}
